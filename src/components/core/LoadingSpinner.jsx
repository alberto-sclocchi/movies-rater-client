import React from 'react'
import loadingSpinner from '../../images/LoadingIcon.webp';

export default function LoadingSpinner() {
  return (
    // <img id="loading-spinner" src={loadingSpinner} alt="Loading..." />
    <div id="loading-spinner"></div>
  )
}
